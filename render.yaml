services:
  - type: web
    name: kashi-eternal-city
    runtime: node
    buildCommand: cd app && npm install && npm run build
    startCommand: cd app && npx serve -s dist -l 10000
    envVars:
      - key: NODE_VERSION
        value: 20
